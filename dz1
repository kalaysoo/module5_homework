let a = +prompt('Введите значение')
if (typeof +a !== 'number') {
  alert('Упс, кажется, вы ошиблись')
} else if (isNaN(a % 2)) {
  alert('Упс, кажется, вы ошиблись')
} else if (a % 2 === 0) {
  alert('Число четное')
} else {
  alert('число нечетное')
}
